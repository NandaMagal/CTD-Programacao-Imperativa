let array = [];
let pares = array.map(function(x));
let impares = array.map(function(x)) {
    return
    for (let i = 0; i <= 100; i++) {
        x = i + numero;
        if (x % 2 == 0) {
            console.log(i + ": o número " + x + " é par");
        } else {
            console.log(i + ": o numero " + x + " não é par");
        }
    }

    console.log(pares);
    console.log(impares)
}

pares(6);
impares(15);